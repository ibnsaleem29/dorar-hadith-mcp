# Dorar Hadith MCP Server

node_modules/
*.log
.env
.DS_Store
*.tmp
*.temp

# Packaging output — regenerated by scripts/build.cjs + `mcpb pack`.
# Ships via GitHub Releases, not git history (also already covered by the
# repo root's build/ ignore rule; listed here again for local clarity).
build/
*.mcpb